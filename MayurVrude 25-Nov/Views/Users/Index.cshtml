@model IEnumerable<MayurVrude_25_Nov.tbluser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.c_username)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.c_usertype)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.c_username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrUsertype)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.c_userid  }) |
                @Html.ActionLink("Details", "Details", new { id = item.c_userid }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.c_userid })
            </td>
        </tr>
    }

</table>
